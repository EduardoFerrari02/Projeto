
import unip.programa.config.Config;
import unip.programa.grafico.JanelaEntrada;

public class Main {
   //Teste
   //Teste2
	public static void main(String args[]) throws InterruptedException {
		/*
		 * byte[] textoencriptado =
		 * Criptografia.getInstance().criptografar("TESTE");
		 * 
		 * String textodecriptado =
		 * Criptografia.getInstance().descriptografar(textoencriptado);
		 * System.out.println("Texto Decriptado: " + textodecriptado);
		 */
		new JanelaEntrada();
		/*Thread tr = new Thread(new Cliente() );
        tr.start();
		tr.sleep(100);*/
		//new Cliente();

	}

}
